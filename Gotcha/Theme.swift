import SwiftUI
import UIKit

enum Theme {

    static let ink = Color(white: 0.06)

    static let paper = Color(white: 1.00)

    static let surface = Color(white: 0.95)

    static let elevated = Color(white: 1.00)

    static let hairline = Color(white: 0.88)

    static let muted = Color(white: 0.42)
}
